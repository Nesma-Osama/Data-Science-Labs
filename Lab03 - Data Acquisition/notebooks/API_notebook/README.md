# Report
## Task 1
I found that the oldest repo across `sklrean`, `pytorch`, `tensorflow` is `sklrean` but the higest stars per day is `tensorflow` means is the most used or popular one and the most issues per stars is `pytorch`

## Task2
I fetched repos from my github account
the public repos is 42 so the are returned is only one page
Most used programming language across all repos: `Jupyter Notebook` and Average stars per repository: around `0.07`,Total forks across all repos: `2`
Most recently updated repo: `BigData`
Oldest repo: `Process-Scheduler-`

## Task3

when search data science with language python stars>=500 I found 30 repos
and compared between `pandas-dev/pandas`,`numpy/numpy`,`scikit-learn/scikit-learn`,`tensorflow/tensorflow`,`pytorch/pytorch`
the highest watch count,forks count and starts is `tensorflow`, the highest issues count pytorch
